document.addEventListener('DOMContentLoaded', (event) => {
    const button = document.getElementById('alertButton');
    
    button.addEventListener('click', () => {
    button.addEventListener('more image', () => {
        alert('Welcome to NGP Wood!');
    });
});
